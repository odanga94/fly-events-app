class CartItem {
    constructor(quantity, eventPrice, eventTitle, sum){
        this.quantity = quantity,
        this.eventPrice = eventPrice,
        this.eventTitle = eventTitle,
        this.sum = sum
    }
}

export default CartItem;